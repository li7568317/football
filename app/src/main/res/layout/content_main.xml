<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="edu.osu.cse.footballkickoff.MainActivity"
    tools:showIn="@layout/activity_main">

    <TextView
        android:id="@+id/showResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize = "40dp"
        android:text="result shows here" />

    <FrameLayout
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:id="@+id/imagePreview"
        android:layout_below="@+id/showResult" />

    <Button
        android:layout_width="120dp"
        android:layout_height="60dp"
        android:text="Generate Seed"
        android:id="@+id/imageGenerateButton"
        android:layout_below="@+id/showResult"
        android:layout_toRightOf="@+id/imagePreview"
        />

    <Button
        android:layout_width="120dp"
        android:layout_height="60dp"
        android:text="Get Seed"
        android:id="@+id/imageGetButton"
        android:layout_toRightOf="@+id/imageGenerateButton"
        android:layout_below="@+id/showResult"
        />


    <Button
        android:layout_width="120dp"
        android:layout_height="60dp"
        android:text="random from sensor"
        android:id="@+id/sensorButton"
        android:layout_below="@+id/imageGenerateButton"
        />



</RelativeLayout>
